// Admin — template approval queue
// Route: /admin/templates
export const dynamic = "force-dynamic";

export default function AdminTemplatesPage() {
  return <div>Admin — Template Review Queue — TODO</div>;
}
